// A bill of materials: import it to align every common-utils module on one version. Its constraints are added by
// configureBom() in the root build script, which lists every other module.
description = "Bill of materials that aligns the versions of every common-utils module"
