.idea/
.gradle/
.wercker/
target/
build/
.kotlin/
.superset/
.DS_Store

*.iml

.jython_cache

# Kotlin/JS-Wasm package lockfiles must be tracked for reproducible builds
# (a user-global ignore may exclude *.lock, so re-include them explicitly).
!kotlin-js-store/**/yarn.lock

